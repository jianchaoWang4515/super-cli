const state = {
    loginInfor: ''
};

const mutations = {
    SET_LOGIN_INFOR (state, data) {
        state.loginInfor = data;
    }
};

const actions = {
};

export default {
    state,
    mutations,
    actions
};
